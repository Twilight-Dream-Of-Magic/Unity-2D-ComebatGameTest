using UnityEngine;

namespace Fighter.Core {
	/// <summary>
	/// Marker component for head label anchoring. Attach to child transform used as head UI anchor.
	/// </summary>
	public class HeadStateAnchor : MonoBehaviour {
	}
}